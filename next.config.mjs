/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['c.myholidays.com', 'www.srilankatailormade.com', 'encrypted-tbn0.gstatic.com'],
      },
};

export default nextConfig;
